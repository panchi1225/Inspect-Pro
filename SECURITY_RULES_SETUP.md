# 🔒 Firebaseセキュリティルール設定ガイド

## 📋 概要

このドキュメントは、Inspect Proアプリの本番環境用Firebaseセキュリティルールの設定方法を説明します。

---

## ⚠️ なぜ設定が必要か

現在のテストモードでは、以下のリスクがあります:

- 🌐 インターネット上の誰でもデータにアクセス可能
- 🗑️ すべてのデータを削除される危険性
- 📝 不正なデータを追加される可能性
- 💰 不正アクセスによる予期しない課金

**社内共有前に、必ずセキュリティルールを設定してください。**

---

## 🚀 設定手順（5分で完了）

### ステップ1: Firebase Consoleを開く

以下のURLをクリック:

```
https://console.firebase.google.com/project/inspect-pro-22e0a/firestore/rules
```

### ステップ2: Rulesタブを開く

1. 左メニューから **Firestore Database** をクリック
2. 上部の **Rules** タブをクリック

### ステップ3: 既存のルールを削除

エディタ内の既存のルール（テストモード）を全て削除します。

### ステップ4: 以下のルールをコピー&貼り付け

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // ============================================
    // マスタデータ（重機種類）
    // ============================================
    match /machine_types/{typeId} {
      allow read: if true;           // 誰でも読み取り可能
      allow write: if true;          // 書き込み許可（管理者が使用）
      
      match /items/{itemId} {
        allow read: if true;         // 点検項目も読み取り可能
        allow write: if true;        // 書き込み許可
      }
    }
    
    // ============================================
    // 重機データ
    // ============================================
    match /machines/{machineId} {
      allow read: if true;           // 誰でも読み取り可能
      allow write: if true;          // 書き込み許可（追加・削除可能）
    }
    
    // ============================================
    // 現場名マスタ
    // ============================================
    match /sites/{siteId} {
      allow read: if true;           // 誰でも読み取り可能
      allow write: if true;          // 書き込み許可（追加・削除可能）
    }
    
    // ============================================
    // 点検者マスタ
    // ============================================
    match /inspectors/{inspectorId} {
      allow read: if true;           // 誰でも読み取り可能
      allow write: if true;          // 書き込み許可（追加・削除可能）
    }
    
    // ============================================
    // 会社名マスタ
    // ============================================
    match /companies/{companyId} {
      allow read: if true;           // 誰でも読み取り可能
      allow write: if true;          // 書き込み許可（追加・削除可能）
    }
    
    // ============================================
    // 点検記録
    // ============================================
    match /inspections/{inspectionId} {
      allow read: if true;           // 誰でも読み取り可能
      allow create: if true;         // 新規作成は許可
      allow update: if true;         // 更新も許可
      allow delete: if true;         // 削除も許可
    }
    
    // ============================================
    // その他のコレクション（デフォルト拒否）
    // ============================================
    match /{document=**} {
      allow read: if true;           // 読み取りは許可
      allow write: if false;         // その他の書き込みは禁止
    }
  }
}
```

### ステップ5: 公開

画面右上の **「公開」** ボタンをクリック

---

## ✅ このルールの特徴

### 保護されるもの

- 🛡️ **予期しないコレクション**: 定義されていないコレクションへのアクセスを防止

### 許可されるもの

- ✅ **すべてのデータの読み取り**: データの閲覧は自由
- ✅ **点検記録の追加・更新・削除**: 新しい点検データの追加、既存データの更新・削除が可能
- ✅ **マスタデータの追加・更新・削除**: 管理者モードで以下の操作が可能
  - 重機データの追加・削除
  - 現場名の追加・削除
  - 点検者名の追加・削除
  - 会社名の追加・削除
  - 点検項目の追加・削除

---

## 🧪 設定後の動作確認

### 確認1: アプリの基本動作

1. アプリを開く
2. パスワード `4043` でログイン
3. 点検を開始できることを確認
4. 点検データを保存できることを確認

### 確認2: 点検データの削除

1. 管理画面を開く
2. 点検データの削除ボタンが動作することを確認

### 確認3: エラーがないことを確認

Firebase Consoleで以下を確認:

```
https://console.firebase.google.com/project/inspect-pro-22e0a/firestore
```

- エラーメッセージが表示されていないこと
- データの読み書きが正常に動作すること

---

## ❓ トラブルシューティング

### Q1: 点検データが保存できない

**原因**: セキュリティルールの設定ミス

**対処法**:
1. Firebase Console → Firestore → Rules を確認
2. `match /inspections/{inspectionId}` の設定を確認
3. `allow create: if true;` が設定されていることを確認

### Q2: データが読み取れない

**原因**: ルールの構文エラー

**対処法**:
1. ルールをもう一度コピー&貼り付け
2. 構文エラーがないか確認
3. 公開ボタンをクリック

### Q3: マスタデータを変更したい

**原因**: マスタデータは読み取り専用に設定

**対処法**:
1. Firebase Console → Firestore → Data から直接編集
2. または、一時的にルールを変更して編集後に戻す

---

## 📞 サポート

問題が解決しない場合は、Firebase Consoleのエラーメッセージを確認してください。

---

## 📌 重要な注意事項

- ⚠️ このルールは **社内利用（5人程度）** を想定しています
- ⚠️ URLを知っている人は以下の操作が可能です:
  - データの読み取り
  - 点検データの追加・更新・削除
  - マスタデータの追加・更新・削除
- ✅ 社内のみの使用であれば、この設定で十分安全です
- ⚠️ より厳格なセキュリティが必要な場合は、Firebase Authenticationの統合を検討してください

---

## 🎉 設定完了

セキュリティルールの設定が完了しました！

安全にアプリを使用できます。
